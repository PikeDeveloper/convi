import 'package:flutter/material.dart';

class EcuationSeparator extends StatelessWidget {
  const EcuationSeparator({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}
